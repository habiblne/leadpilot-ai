# Server-side only. Both values are required; never use a public environment-variable prefix.
N8N_WEBHOOK_URL=
# Use the same random value (minimum 32 characters) in Vercel and n8n Header Auth.
N8N_WEBHOOK_SECRET=
